# 学习工具功能测试指南

## 测试环境准备

1. 确保 `.dev.vars` 文件中配置了 `OPENROUTER_API_KEY`
2. 启动开发服务器: `npm run dev`
3. 创建或打开一个学习计划

## 测试步骤

### 1. 准备测试文档

1. 在学习计划中创建一个新文档
2. 添加一些学习内容（至少 100 字）
3. 示例内容:
   ```
   JavaScript 闭包

   闭包是指函数可以访问其外部作用域的变量，即使外部函数已经执行完毕。

   闭包的特点：
   1. 函数嵌套函数
   2. 内部函数引用外部函数的变量
   3. 外部函数返回内部函数

   闭包的应用场景：
   - 数据私有化
   - 函数柯里化
   - 模块化开发
   ```

### 2. 测试闪卡生成

1. 点击右侧学习工具侧边栏中的"生成闪卡"
2. 点击"展开"查看详情
3. 点击"AI 生成"按钮
4. 等待生成完成（约 5-10 秒）
5. 应该看到提示: "成功生成 X 张闪卡"

**预期结果:**
- 生成 5-10 张闪卡
- 每张闪卡包含问题和答案
- 闪卡保存到数据库

**验证方法:**
```bash
# 查询数据库中的闪卡
wrangler d1 execute DB --command "SELECT * FROM flashcards ORDER BY created_at DESC LIMIT 10"
```

### 3. 测试复习计划生成

1. 点击"创建复习计划"
2. 点击"展开"查看详情
3. 点击"AI 生成"按钮
4. 等待生成完成（约 1-2 秒）
5. 应该看到提示: "已为该内容生成 7 轮复习计划"

**预期结果:**
- 生成 7 轮复习计划
- 复习时间点符合艾宾浩斯遗忘曲线
- 计划保存到数据库

**验证方法:**
```bash
# 查询数据库中的复习计划
wrangler d1 execute DB --command "SELECT * FROM review_schedules ORDER BY created_at DESC LIMIT 10"
```

### 4. 测试费曼学习法（新版本 - 用户主导）

1. 点击"费曼学习法"
2. 点击"展开"查看详情
3. 点击"AI 生成"按钮
4. 等待 AI 提取核心概念（约 5-8 秒）
5. 应该看到提示: "已提取 X 个核心概念"
6. 自动打开费曼概念对话框，显示概念列表
7. 选择一个概念（点击概念卡片）
8. 在文本框中用自己的话解释这个概念
9. 点击"提交并获取 AI 反馈"按钮
10. 等待 AI 评估（约 5-8 秒）
11. 查看右侧的 AI 反馈：
    - 理解程度评分（0-100）
    - 需要改进的地方（知识盲点）
    - 改进建议

**预期结果:**
- AI 提取 3-5 个核心概念
- 每个概念包含名称、描述和难度级别
- 用户可以选择任意概念进行解释
- AI 准确评估用户的理解程度
- 提供有针对性的改进建议
- 解释和反馈保存到数据库

**验证方法:**
```bash
# 查询数据库中的费曼解释
wrangler d1 execute DB --command "SELECT concept, explanation, ai_feedback FROM feynman_explanations ORDER BY created_at DESC LIMIT 5"
```

**测试要点:**
1. **概念提取质量**: AI 提取的概念是否准确、重要
2. **用户体验**: 对话框是否易用、流程是否流畅
3. **AI 评估准确性**: AI 反馈是否有针对性、是否有帮助
4. **多次练习**: 可以返回选择其他概念继续练习

**示例解释（用于测试）:**

**好的解释（应该得高分）:**
```
闭包就像一个背包，函数在创建时会把它需要的外部变量装进这个背包里。
即使函数离开了它的出生地（外部函数执行完毕），它仍然可以从背包里
拿出这些变量来使用。

举个例子：想象你在家里（外部函数）准备了一个便当盒（内部函数），
里面装了食物（外部变量）。即使你离开家去公司了（外部函数执行完毕），
你仍然可以打开便当盒吃到食物（内部函数访问外部变量）。

这就是为什么闭包可以用来保护数据，因为外部无法直接访问"背包"里的东西，
只能通过函数提供的"接口"来使用。
```

**差的解释（应该得低分）:**
```
闭包就是函数可以访问外部变量。
```

### 5. 测试康奈尔笔记生成

1. 点击"康奈尔笔记"
2. 点击"展开"查看详情
3. 点击"AI 生成"按钮
4. 等待生成完成（约 5-8 秒）
5. 应该看到提示: "康奈尔笔记已生成并保存"

**预期结果:**
- 主笔记区: 文档内容
- 线索区: AI 生成的关键词和问题
- 总结区: AI 生成的 2-3 句话总结
- 笔记保存到数据库

**验证方法:**
```bash
# 查询数据库中的康奈尔笔记
wrangler d1 execute DB --command "SELECT * FROM cornell_notes ORDER BY created_at DESC LIMIT 5"
```

## 常见问题

### 1. 提示"AI 功能暂不可用"
- 检查 `.dev.vars` 文件中是否配置了 `OPENROUTER_API_KEY`
- 检查 API Key 是否有效

### 2. 提示"文档内容太少"
- 确保文档内容至少有 50 个字符
- 添加更多学习内容

### 3. 生成失败
- 查看浏览器控制台的错误信息
- 查看服务器日志
- 检查网络连接

### 4. 生成速度慢
- AI 生成需要时间，请耐心等待
- 费曼解释生成时间最长（10-15 秒）
- 复习计划生成最快（1-2 秒）

## 性能指标

| 工具 | 预期生成时间 | 生成数量 |
|------|------------|---------|
| 闪卡 | 5-10 秒 | 5-10 张 |
| 复习计划 | 1-2 秒 | 7 轮 |
| 费曼学习法 | 5-8 秒（提取概念）+ 5-8 秒（评估解释）| 3-5 个概念 |
| 康奈尔笔记 | 5-8 秒 | 1 份笔记 |

## 成功标准

- ✅ 所有 4 种工具都能成功生成
- ✅ 生成的内容质量高、准确
- ✅ 数据正确保存到数据库
- ✅ 用户体验流畅，提示清晰
- ✅ 错误处理完善，失败时有明确提示
