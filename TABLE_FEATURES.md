# 表格功能说明

## 当前支持的功能 ✅

### 1. 创建表格
- 使用斜杠命令 `/表格` 创建 3x3 表格（带表头）
- 默认创建 3 行 3 列

### 2. 添加行列
- **添加行**：
  - 在上方添加行（ArrowUp 图标）
  - 在下方添加行（ArrowDown 图标）
- **添加列**：
  - 在左侧添加列（ArrowLeft 图标）
  - 在右侧添加列（ArrowRight 图标）

### 3. 删除行列
- 删除当前行（Minus 图标）
- 删除当前列（Trash 图标）
- 删除整个表格（Trash 图标）

### 4. 调整列宽 ✅
- 鼠标悬停在列边框上
- 拖动边框调整列宽
- 支持精确调整

### 5. 表格样式
- 表头：灰色背景，粗体文字
- 单元格：白色背景，边框
- 选中单元格：青绿色高亮

### 6. 工具栏跟随滚动 ✅ (NEW!)
- 表格工具栏现在会随着页面滚动自动更新位置
- 始终显示在光标附近

## 不支持的功能 ⚠️

### 1. 行高调整
**原因**：
- TipTap 的表格扩展不支持行高调整
- 行高由单元格内容自动决定
- 这是大多数文档编辑器的标准行为

**替代方案**：
- 在单元格中添加更多内容来增加行高
- 使用换行符（Shift + Enter）增加行高
- 使用 CSS 样式（需要自定义扩展）

### 2. 合并单元格
**原因**：
- 需要自定义表格扩展
- 增加复杂性

**计划**：
- 可以在未来版本中添加

### 3. 单元格背景色
**原因**：
- 需要自定义表格扩展

**计划**：
- 可以在未来版本中添加

## 使用指南

### 创建表格
1. 输入 `/表格`
2. 选择"表格"命令
3. 自动创建 3x3 表格

### 编辑表格
1. 点击表格中的任意单元格
2. 表格工具栏自动出现
3. 使用工具栏按钮进行操作

### 调整列宽
1. 鼠标悬停在列边框上
2. 光标变为调整大小图标
3. 拖动边框调整宽度

### 删除表格
1. 点击表格中的任意单元格
2. 点击工具栏最右侧的删除图标
3. 整个表格被删除

## 表格快捷键

| 快捷键 | 功能 |
|--------|------|
| Tab | 移动到下一个单元格 |
| Shift + Tab | 移动到上一个单元格 |
| Enter | 在单元格内换行 |
| Ctrl + Enter | 在表格后添加段落 |

## 表格样式自定义

如果需要自定义表格样式，可以修改 `src/app/globals.css` 中的表格样式：

```css
/* 表格样式 */
.ProseMirror table {
  border-collapse: collapse;
  table-layout: fixed;
  width: 100%;
  margin: 1rem 0;
}

.ProseMirror table td,
.ProseMirror table th {
  min-width: 1em;
  border: 2px solid var(--color-border-light);
  padding: 0.5rem 0.75rem;
  vertical-align: top;
}

.ProseMirror table th {
  font-weight: 600;
  text-align: left;
  background-color: rgba(13, 148, 136, 0.05);
}
```

## 已修复的问题 ✅

1. ✅ **工具栏不随滚动更新**：现在工具栏会随着页面滚动自动更新位置
2. ✅ **列宽调整**：支持拖动列边框调整宽度

## 未来计划 🚀

1. **合并单元格**：支持合并多个单元格
2. **单元格背景色**：为单元格添加背景颜色
3. **表格模板**：预设的表格样式模板
4. **导出表格**：导出为 CSV、Excel 格式
5. **表格排序**：点击表头排序数据

## 技术说明

### 为什么不支持行高调整？

1. **内容驱动**：表格行高应该由内容决定，而不是手动设置
2. **一致性**：保持与其他文档编辑器的一致行为
3. **简单性**：避免增加不必要的复杂性
4. **响应式**：自动行高更适合响应式设计

### 如何增加行高？

**方法 1：添加内容**
```
在单元格中添加更多文本
或使用 Shift + Enter 添加换行
```

**方法 2：使用 CSS（高级）**
如果确实需要固定行高，可以在 `globals.css` 中添加：

```css
.ProseMirror table td,
.ProseMirror table th {
  min-height: 50px; /* 设置最小行高 */
}
```

## 反馈和建议

如果你有任何关于表格功能的建议或需求，请告诉我！
