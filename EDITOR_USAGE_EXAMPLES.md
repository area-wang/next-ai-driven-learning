# 富文本编辑器使用示例

## 快速开始

### 1. 创建基础文档

```
# 我的第一篇文档

这是一段普通文本。你可以使用 **粗体**、*斜体* 和 ~~删除线~~。

## 添加列表

无序列表：
- 项目 1
- 项目 2
- 项目 3

有序列表：
1. 第一步
2. 第二步
3. 第三步

任务列表：
- [ ] 待办事项 1
- [x] 已完成事项
- [ ] 待办事项 2
```

### 2. 使用提示框

输入 `/信息` 创建信息提示框：

> ℹ️ **提示**：这是一个信息提示框，用于显示重要信息。

输入 `/警告` 创建警告提示框：

> ⚠️ **警告**：这是一个警告提示框，用于提醒用户注意事项。

输入 `/成功` 创建成功提示框：

> ✅ **成功**：这是一个成功提示框，用于显示操作成功的消息。

输入 `/错误` 创建错误提示框：

> ❌ **错误**：这是一个错误提示框，用于显示错误信息。

### 3. 插入代码块

输入 `/代码` 创建代码块：

\`\`\`javascript
function hello() {
  console.log("Hello, World!");
}
\`\`\`

### 4. 创建表格

输入 `/表格` 创建 3x3 表格：

| 列 1 | 列 2 | 列 3 |
|------|------|------|
| 数据 | 数据 | 数据 |
| 数据 | 数据 | 数据 |

### 5. 添加数学公式

输入 `/数学` 插入 LaTeX 公式：

行内公式：$E = mc^2$

块级公式：
$$
\int_{a}^{b} f(x) dx = F(b) - F(a)
$$

### 6. 使用颜色

1. 选中文本
2. 点击浮动工具栏的调色板图标
3. 选择字体颜色或背景颜色

示例：
- <span style="color: #EF4444">红色文字</span>
- <span style="background-color: #FEF3C7">黄色背景</span>

### 7. 插入媒体

**图片**：
- 输入 `/图片` 然后输入 URL
- 或直接拖拽图片文件
- 或复制粘贴图片

**视频**：
- 输入 `/视频` 然后输入 YouTube 或 Vimeo URL

## 高级技巧

### 1. 快速格式化

选中文本后使用键盘快捷键：
- `Ctrl/Cmd + B`：粗体
- `Ctrl/Cmd + I`：斜体
- `Ctrl/Cmd + Shift + X`：删除线

### 2. 列表嵌套

在列表项中按 `Tab` 创建子列表：

- 父项目
  - 子项目 1
  - 子项目 2
    - 孙项目

### 3. 表格编辑

在表格中右键打开菜单：
- 添加行/列
- 删除行/列
- 拖动列边框调整宽度

### 4. 链接插入

1. 选中文本
2. 点击浮动工具栏的链接图标
3. 输入 URL

或使用 Markdown 语法：`[文本](URL)`

### 5. 引用块

输入 `/引用` 创建引用块：

> 这是一段引用文字。
> 可以跨多行。

## 常见场景

### 场景 1：编写技术文档

```markdown
# API 文档

## 概述
这是一个 RESTful API 接口文档。

## 端点

### GET /api/users

获取用户列表。

**请求参数**：
- `page`：页码（可选）
- `limit`：每页数量（可选）

**响应示例**：
\`\`\`json
{
  "users": [
    { "id": 1, "name": "张三" }
  ]
}
\`\`\`

> ℹ️ **提示**：默认返回前 10 条记录。
```

### 场景 2：编写学习笔记

```markdown
# JavaScript 学习笔记

## 变量声明

JavaScript 有三种变量声明方式：

1. `var`：函数作用域
2. `let`：块作用域
3. `const`：块作用域（常量）

> ⚠️ **注意**：推荐使用 `let` 和 `const`，避免使用 `var`。

## 示例代码

\`\`\`javascript
const name = "张三";
let age = 25;
console.log(\`\${name} 今年 \${age} 岁\`);
\`\`\`

## 任务清单

- [x] 学习变量声明
- [ ] 学习函数
- [ ] 学习对象
```

### 场景 3：编写项目计划

```markdown
# 项目开发计划

## 第一阶段：需求分析

**时间**：2024-01-01 ~ 2024-01-15

**任务**：
- [ ] 收集用户需求
- [ ] 编写需求文档
- [ ] 评审需求

> ✅ **里程碑**：完成需求文档

## 第二阶段：设计

**时间**：2024-01-16 ~ 2024-01-31

**任务**：
- [ ] UI 设计
- [ ] 数据库设计
- [ ] API 设计

## 风险提示

> ⚠️ **风险**：需求变更可能导致延期。
```

## 最佳实践

### 1. 文档结构

使用标题层级组织内容：
- H1：文档标题
- H2：主要章节
- H3：子章节
- H4-H6：更细的层级

### 2. 视觉层次

使用不同的内容块增强可读性：
- 提示框：重要信息
- 引用块：引用内容
- 代码块：代码示例
- 表格：结构化数据

### 3. 颜色使用

适度使用颜色突出重点：
- 红色：错误、警告
- 绿色：成功、正确
- 蓝色：信息、链接
- 黄色：高亮、注意

### 4. 列表使用

选择合适的列表类型：
- 无序列表：无顺序的项目
- 有序列表：有步骤的流程
- 任务列表：待办事项

## 故障排除

### 问题 1：颜色继承

**症状**：应用颜色后，后续输入也是该颜色

**解决**：这个问题已修复。如果仍然出现，请刷新页面。

### 问题 2：工具栏不显示

**症状**：选中文本后工具栏不出现

**解决**：确保选中了文本（不是空选区）。

### 问题 3：图片上传失败

**症状**：拖拽图片后显示错误

**解决**：
1. 检查图片大小（建议 < 5MB）
2. 检查图片格式（支持 JPG、PNG、GIF）
3. 检查网络连接

## 更多资源

- [TipTap 官方文档](https://tiptap.dev/)
- [Markdown 语法指南](https://www.markdownguide.org/)
- [LaTeX 数学公式](https://katex.org/)
