# AI 编辑器集成完成

## 功能概述

已成功将完整的文档编辑器（含 AI 生成功能）集成到学习计划详情页面，同时保留 test-editor 页面作为测试用途。

## 页面说明

### 1. 学习计划详情页 (`/learn/[planId]`)
- **用途**：正式的学习计划编辑页面
- **路由**：`/learn/[planId]`（例如：`/learn/1`）
- **特点**：
  - 顶部显示"学习计划 #[planId]"
  - 有返回按钮（ChevronLeft）
  - 完整的文档树 + 编辑器 + AI 生成功能
  - 用于实际的学习计划管理

### 2. 测试编辑器页 (`/test-editor`)
- **用途**：测试和演示编辑器功能
- **路由**：`/test-editor`
- **特点**：
  - 顶部显示"文档编辑器"
  - 无返回按钮
  - 完整的文档树 + 编辑器 + AI 生成功能
  - 用于功能测试和演示

## 核心功能（两个页面相同）

### 左侧：文档树
- 显示文档的树形结构
- 支持添加、删除文档
- 每个文档节点有 AI 生成按钮（紫色 Sparkles 图标）
- 支持嵌套子文档
- 可折叠/展开

### 中间：富文本编辑器
- 支持标题和内容编辑
- 斜杠命令（输入 `/` 打开命令菜单）
- 支持拖拽上传图片、视频
- 支持数学公式、代码块
- 浮动工具栏（选中文本时显示）

### 右侧：内容大纲
- 自动提取文档中的标题（h1-h6）
- 按层级组织显示
- 点击跳转到对应标题
- 支持展开/收起

### 顶部：工具栏
- AI 生成内容按钮（全局）
- 显示生成进度

## AI 生成功能

### 两种生成方式：
1. **顶部工具栏生成**：在根级别生成新章节
2. **文档树节点生成**：为指定章节生成子内容

### AI 生成对话框
- 输入学习主题
- 输入学习目标（可选）
- 选择难度级别（初级/中级/高级）
- 显示生成进度

### 自动转换
- AI 大纲 → 文档树结构
- AI 内容 → HTML 格式
- 支持多层级嵌套
- 自动生成标题、描述、学习时间、前置知识

## 页面布局

```
┌─────────────────────────────────────────────────────────────┐
│  ← 返回  |  学习计划 #1  |           |  AI 生成内容         │
├──────────┬──────────────────────────────────────┬───────────┤
│          │                                      │           │
│  文档树  │         富文本编辑器                  │  内容大纲  │
│          │                                      │           │
│  ├ 第1章 │  ┌────────────────────────────────┐  │  ├ 标题1  │
│  │ ├1.1  │  │                                │  │  ├ 标题2  │
│  │ └1.2  │  │     编辑区域                   │  │  └ 标题3  │
│  ├ 第2章 │  │                                │  │           │
│  │ ├2.1  │  └────────────────────────────────┘  │           │
│  │ └2.2  │                                      │           │
│  └ [+]   │                                      │           │
└──────────┴──────────────────────────────────────┴───────────┘
```

## 技术细节

- 使用 `useParams` 获取 planId
- 使用 `useAIConfig` hook 获取用户配置的 AI 模型和 API Key
- 支持多种 AI 提供商（OpenAI、DeepSeek、Gemini、Claude、Cloudflare AI）
- 递归处理嵌套大纲结构
- 自动生成 HTML 格式内容
- 类型安全的 API 响应处理

## 已验证

✅ TypeScript 类型检查通过
✅ Next.js 编译成功（无错误）
✅ 两个页面都正常工作
✅ 所有组件正确导入和使用
✅ AI 配置正确集成
✅ 文档树和编辑器功能完整
✅ AI 生成功能正常工作

## 使用方式

### 访问学习计划详情页
```
http://localhost:3000/learn/1
http://localhost:3000/learn/2
...
```

### 访问测试编辑器页
```
http://localhost:3000/test-editor
```

## 代码复用

两个页面使用相同的组件：
- `DocumentTree` - 文档树组件
- `TiptapEditor` - 富文本编辑器
- `ContentOutline` - 内容大纲
- `AIGenerateDialog` - AI 生成对话框

唯一的区别是：
- 学习计划详情页有返回按钮和 planId 显示
- test-editor 页面是独立的测试页面
