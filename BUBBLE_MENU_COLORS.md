# 浮动工具栏颜色选择器使用说明

## 功能特点

### 1. 合并的颜色选择器
- 字体颜色和背景颜色在同一个面板中
- 点击调色板图标打开颜色选择面板

### 2. 预设颜色
**字体颜色（8种）：**
- 黑色 (#1F2937)
- 灰色 (#6B7280)
- 红色 (#EF4444)
- 橙色 (#F97316)
- 黄色 (#D97706)
- 绿色 (#10B981)
- 蓝色 (#3B82F6)
- 紫色 (#8B5CF6)

**背景颜色（8种浅色系）：**
- 无背景（透明）
- 浅灰 (#F3F4F6)
- 浅红 (#FEE2E2)
- 浅橙 (#FFEDD5)
- 浅黄 (#FEF3C7)
- 浅绿 (#D1FAE5)
- 浅蓝 (#DBEAFE)
- 浅紫 (#EDE9FE)

### 3. 使用方法
1. 选中文本
2. 浮动工具栏自动出现
3. 点击调色板图标
4. 选择字体颜色或背景颜色
5. 颜色应用到选中文本，选中状态保持
6. 点击编辑器其他位置取消选中
7. 工具栏自动消失
8. 继续输入新文本，不会继承颜色

### 4. 技术实现
- 使用 `removeStoredMark` 清除 mark 状态，防止颜色继承
- 应用颜色后保持选中状态
- **严格规则：只要没有选中文本，就立即隐藏工具栏**
- 自定义 `::selection` 样式避免与背景色冲突
- 使用半透明主色调作为选中背景

### 5. 工具栏显示/隐藏逻辑（简化版）
- **显示**：有选中文本时
- **隐藏**：没有选中文本时（立即隐藏，不管鼠标位置）

这个逻辑与其他工具（粗体、斜体等）完全一致，颜色选择没有特殊处理。

## 已修复的问题
✅ 字体颜色只应用到选中文本，不影响后续输入
✅ 背景颜色只应用到选中文本，不影响后续输入（使用 removeStoredMark）
✅ 应用颜色后保持选中状态
✅ 取消选中后工具栏立即消失（与其他功能一致）
✅ 颜色选择与其他工具行为完全一致，没有特殊处理
✅ 浏览器默认选中样式不再与自定义背景色冲突
✅ 下拉菜单 hover 交互流畅
✅ 移除了自定义颜色选择器，简化操作

## 测试步骤
1. 打开 `/test-editor` 页面
2. 输入一些文本："这是测试文本，用来测试颜色功能"
3. 选中部分文字（例如"测试文本"）
4. 工具栏出现 ✓
5. 点击调色板图标
6. 选择一个字体颜色（例如红色）
7. 验证：
   - 选中的文字变成红色 ✓
   - 选中状态保持 ✓
   - 工具栏仍然显示 ✓
8. 点击编辑器其他位置，取消选中
9. 验证：工具栏立即消失 ✓
10. 在红色文字后面继续输入新文字
11. 验证：新输入的文字是默认颜色（黑色），不是红色 ✓
12. 再次选中文字，选择背景色（例如浅黄）
13. 验证：
    - 背景色应用到选中文字 ✓
    - 选中状态保持 ✓
14. 取消选中
15. 验证：工具栏消失 ✓
16. 继续输入
17. 验证：新输入的文字无背景色 ✓

## 关键代码说明
```typescript
// 严格规则：只要没有选中文本，就隐藏工具栏
if (empty || from === to) {
  setIsVisible(false)
  setActiveCategory(null)
  return
}
```

这个简单的规则确保工具栏的行为与所有其他功能完全一致。

```typescript
// 清除 mark 状态，但保持选中
editor.view.dispatch(
  editor.state.tr.removeStoredMark(editor.schema.marks.highlight)
)
```

这个方法直接操作 ProseMirror 的 transaction，清除存储的 mark，确保后续输入不会继承颜色。



